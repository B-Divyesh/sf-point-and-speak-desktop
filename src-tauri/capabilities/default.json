{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the local capture window",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-hide", "global-shortcut:default"]
}
