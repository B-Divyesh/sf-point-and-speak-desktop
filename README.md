# Point & Speak Desktop

Live: https://point-and-speak-desktop.sociobot.in — built by the Param Factory (`desktop-app`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
